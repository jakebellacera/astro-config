---
const title = 'Welcome to Astro';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        max-width: 800px;
        margin: 0 auto;
        padding: 2rem;
        line-height: 1.6;
      }
      h1 {
        color: #4f46e5;
      }
      .config {
        background: #f3f4f6;
        padding: 1rem;
        border-radius: 0.5rem;
        margin: 1rem 0;
      }
      pre {
        overflow-x: auto;
      }
    </style>
  </head>
  <body>
    <h1>{title}</h1>
    <p>
      This is a basic example of an Astro site using the
      <code>astro-config</code> integration.
    </p>

    <h2>Integration Configuration</h2>
    <div class="config">
      <p>
        The astro-config integration has been configured with custom options.
      </p>
      <p>Check the browser console to see the integration messages.</p>
      <pre><code>{JSON.stringify(
          {
            appName: 'Basic Example',
            version: '1.0.0',
            features: {
              analytics: true,
              darkMode: true,
            },
          },
          null,
          2
        )}</code></pre>
    </div>

    <h2>Next Steps</h2>
    <ul>
      <li>Edit <code>astro.config.mjs</code> to customize the integration</li>
      <li>
        Modify <code>src/pages/index.astro</code> to update this page
      </li>
      <li>Add more pages to your site</li>
    </ul>
  </body>
</html>
